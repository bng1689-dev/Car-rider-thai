# จำเป็น
PyYAML>=6.0          # อ่านไฟล์อ้างอิง L0 (มีคอมเมนต์อธิบายเกณฑ์ได้)
openpyxl>=3.1        # สร้าง .xlsx

# ไม่บังคับ — ขาดแล้ว build ชนิดอื่นยังทำงานได้ ข้ามเฉพาะ pdf
weasyprint>=60       # สร้าง .pdf (ต้องมีฟอนต์ Sarabun / TH SarabunPSK ในระบบ)
